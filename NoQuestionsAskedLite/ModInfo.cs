using ICities;

namespace NoQuestionsAskedLite
{
    public class ModInfo : IUserMod
    {
        public string Name => "No Questions Asked Lite";
        public string Description => "Changes the default bulldozer behavior on assets that require user confirmation when bulldozing";
    }
}
